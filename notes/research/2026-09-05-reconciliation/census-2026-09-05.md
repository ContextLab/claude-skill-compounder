# Census — claude-skill-compounder, 2026-09-05

Repo `/Users/jmanning/claude-skill-compounder`, `main` at **603e61a**. READ-ONLY: no repo
file was edited; the only things written under `~/.claude` are what the read-only CLIs
(`skillreport`, `skillrepeat`, `skillcontrib recon`) and the ambient hooks write for
themselves.

Measured against the maintainer's 2026-09-03 gate: *"I would resist adding any new gate or
subsystem until: CI is green, one complete end-to-end scenario is repeatable, usage is
measured across several repositories, and an existing component is retired or simplified to
make room."*

Every figure below appears verbatim in the output of the command printed beside it.

---

## 1. Genuine use per shipped skill

Commands:

```bash
bash bin/skillreport skills            # saved to scratchpad/skillreport-skills.txt (423 lines)
bash bin/skillreport --help
jq -r 'select(.event=="use") | [.name, .harness, .project] | @tsv' \
  ~/.claude/skill-compounder/ledger.jsonl | sort | uniq -c   # -> scratchpad/use-by-project.txt (781 lines)
```

Per-skill counts (`.harness==false` = genuine, `true` = harness, absent = unclassified):

```bash
for s in $(ls skills/); do
  jq -r --arg s "$s" 'select(.event=="use" and .name==$s) |
    (if (.harness==false) then "G" elif (.harness==true) then "H" else "U" end)' \
    ~/.claude/skill-compounder/ledger.jsonl |
  awk -v s="$s" '{c[$1]++} END{printf "%-30s %3d %3d %3d\n", s, c["G"], c["H"], c["U"]}'
done
for s in $(ls skills/); do
  jq -r --arg s "$s" 'select(.event=="use" and .name==$s and .harness==false) |
    [(.ts|todate), .project] | @tsv' ~/.claude/skill-compounder/ledger.jsonl | sort | ...
done
```

|skill|genuine|harness|uncl.|first genuine|last genuine|distinct projects|genuine uses OUTSIDE this repo|
|-|-|-|-|-|-|-|-|
|ai-tell-audit|43|65|0|2026-08-26|2026-09-05|5|9 (hypertools 6, faculty-leave-grant 1, kalaniyot-website 1, cdl-linux 1)|
|claim-provenance|20|64|0|2026-08-26|2026-09-05|2|1 (cdl-linux)|
|contribute-skill|0|33|0|—|—|0|**0 (never genuinely used anywhere)**|
|dead-guard-detection|1|33|0|2026-08-26|2026-08-26|1|**0**|
|destructive-op-preflight|3|33|0|2026-08-26|2026-09-04|2|1 (hypertools)|
|finish-task|1|84|0|2026-08-27|2026-08-27|1|**0**|
|no-silent-stub|1|40|0|2026-08-26|2026-08-26|1|**0**|
|parallel-agents-one-codebase|3|24|0|2026-09-02|2026-09-05|2|1 (hypertools)|
|session-handoff|2|33|0|2026-08-26|2026-09-03|1|**0**|
|skill-authoring|17|41|0|2026-08-26|2026-09-05|1|**0**|
|skill-compounder|8|82|0|2026-08-26|2026-09-05|3|2 (writing-style-guide 1, cdl-linux 1)|
|stale-artifact-check|5|42|17|2026-08-26|2026-09-01|1|**0**|

Aggregate (verified in one command):

```bash
S=$(ls skills/ | paste -sd'|' -)
jq -r --arg re "^($S)$" 'select(.event=="use" and (.name|test($re))) |
  (if (.harness==false) then "genuine" elif (.harness==true) then "harness" else "unclassified" end)' \
  ~/.claude/skill-compounder/ledger.jsonl | sort | uniq -c
#  104 genuine / 574 harness / 17 unclassified
jq -r --arg re "^($S)$" 'select(.event=="use" and .harness==false and (.name|test($re))) |
  (if .project=="/Users/jmanning/claude-skill-compounder" then "in-repo" else "outside" end)' ... 
#   90 in-repo / 14 outside
```

- **104 genuine uses vs 574 harness uses** — 5.5 harness invocations per genuine one. The
  routing probes and tests dominate the record.
- **90 of 104 genuine uses (86.5%) are inside this repository.** 14 are outside, spread
  over 5 projects: hypertools 8, cdl-linux 3, writing-style-guide 1, kalaniyot-website 1,
  faculty-leave-grant 1.
- **Seven of twelve shipped skills have ZERO genuine use outside this repository**:
  `contribute-skill` (zero genuine use anywhere, 33 harness), `dead-guard-detection`,
  `finish-task`, `no-silent-stub`, `session-handoff`, `skill-authoring`,
  `stale-artifact-check`.
- Only **two** skills have genuine use in more than one outside project: `ai-tell-audit`
  (4 outside projects) and `skill-compounder` (2).
- Four skills have been genuinely used exactly **once, ever**: `dead-guard-detection`,
  `finish-task`, `no-silent-stub` (all last used 2026-08-26/27, nine days ago).
- Ledger horizon (printed by `skillreport skills`): *"the earliest row is 2026-08-25.
  Whether anything happened before that is UNKNOWN, not none"*.
- `skillreport skills` "applied" column: **3 skills carry closed forges with NO apply row**
  (`ai-tell-audit` 2 forges, `claim-provenance` 1, `skill-compounder` 1). Exactly **one**
  `apply` row exists in the whole ledger:
  `jq -c 'select(.event=="apply")|{name,outcome}'` -> `{"name":"finish-task","outcome":"used"}`.

Against the maintainer's third condition — *usage measured across several repositories* —
the instrument exists and has been read; the answer is that usage is **not** across several
repositories. It is one repository plus a thin tail.

---

## 2. Which mechanisms have ever fired for real

|mechanism|deliveries / fires|outcomes attributable|evidence command|
|-|-|-|-|
|repeat REFUSAL arm (`REPEAT_GATE_REFUSE`)|**0 — never fired, ever**|none; switch is unset (default `0`), and the arm's only marker tree `repeats/denied/` does not exist|`[ -d ~/.claude/skill-compounder/repeats/denied ]` -> absent; `grep -n 'REFUSE=' hooks/repeat-gate.sh` -> `REFUSE="${REPEAT_GATE_REFUSE:-0}"` (line 974); marker write at line 2935 `mkdir -p "$DIR/denied/$sid"`|
|lesson gate — statement arm (`said-`)|**175 statements over 21 sessions**|21 sessions saw at least one|`find ~/.claude/skill-compounder/repeats/lessons -name 'said-*' \| wc -l`|
|lesson gate — armed (`s-`)|**8 markers, 8 sessions**|—|`find .../repeats/lessons -name 's-*' -maxdepth 2 \| wc -l`|
|lesson gate — DENIES|**5 denies over 5 sessions, 2 signatures**|4 of 5 on one synthetic red-team signature `c2602516446x45-e345528415x49` (`python3 scripts/count_words.py README.md`); 1 on `c3861778000x28` (`ls <P>/alpha-beta-gamma`) — both are red-team fixtures, not real work|`find .../repeats/lessons -path '*/deny/*' -mindepth 3 -maxdepth 3 -type d`|
|`hooks/doc-gate.sh`|**4 denies** (distinct session+HEAD)|2 overrides taken, both in OTHER repos (cdl-linux, hypertools), each with a written reason|`find ~/.claude/skill-compounder/doc-gate/denied -mindepth 2 -type f \| wc -l`; `cat .../doc-gate/overrides.jsonl`|
|`hooks/apply-gate.sh`|**1 block**|the one block named `finish-task`, and `finish-task` is the one skill with an `apply` row (`outcome:"used"`)|`ls ~/.claude/skill-compounder/apply-gate \| grep named` -> `25a4770c-….named.finish-task` (20 other entries are `.turn` claims, taken before any block)|
|`hooks/claim-gate.sh` (bonus)|**20 `.blocked` Stop refusals + 6 `deny.` PreToolUse commit refusals**|2 of the 20 are probe sessions (`prodloop-claim-probe-3/4`)|`ls ~/.claude/skill-compounder/claim-gate \| grep -c '.blocked$'`|
|paid session review|**6 runs, $0.8813022 total**|**3 CANDIDATE, 3 NONE.** Of the 3 candidates: 1 is `check-test-pinned-strings-before-editing-skill-prose`, which became global note `n64622848x189`; the other two (`kill-and-rerun-full-suite`, `orchestrator-sendmessage-delivery-unreliable`) produced no skill and no note. Ran in 5 different projects.|`jq -s 'map(.cost_usd\|tonumber)\|add' ~/.claude/skill-compounder/reviews/index.jsonl`; `jq -r '.verdict' … \| sort \| uniq -c`|
|`skillinsight` promotions|**3 distinct candidate ids promoted** (12 log rows; duplicates)|`n1155178185x138`, `n735026689x210`, `n1166131302x139` — the last two are visible in the global CLAUDE.md notes block|`jq -r '.id' ~/.claude/skill-compounder/insights/.promoted.jsonl \| sort \| uniq -c`|
|insight queue backlog|**66 candidates** (W35 30 + W36 36); 46 declined rows|sources: 47 star-insight, 18 session-audit, 1 marker|`wc -l ~/.claude/skill-compounder/insights/2026-W3*.jsonl`; `cat … \| jq -r '.source' \| sort \| uniq -c`|
|prompt-arm nudge (`ci-skill-check`)|**175 rows in nudges.jsonl** (of 419 total: checkpoint 207, skill-check 175, prose 37). Nudge log only starts **2026-09-04T03:06:47Z**.|see below|`jq -r '.id' ~/.claude/skill-compounder/reminders/nudges.jsonl \| sort \| uniq -c`|
|↳ conversion, ALL sessions|1126 nudged sessions, 1411 skill-check deliveries seen in transcripts|nudge→any output **108 / 1126 = 9.59%**; nudge→skill **100 = 8.88%**; nudge→tier CLI **11 = 0.98%**|`python3 scripts/reminder_conversion.py --json`|
|↳ conversion, HUMAN-DRIVEN|**19 nudged sessions**|nudge→any output **12 / 19 = 63.2%**; nudge→skill **6 = 31.6%**; nudge→tier CLI **9 = 47.4%**; `invoked_without_a_nudge` = 0|same|
|mission (`hooks/mission.sh`)|**380 deliveries over 17 sessions**, all within **2026-09-04T00:57:18Z → 2026-09-06T00:24:33Z** (~2 days)|by moment: dispatch 136, subagent 135, periodic 69, completion 21, ambiguity 10, resume 9 — all five moments fire|`jq -r '.moment' ~/.claude/skill-compounder/mission/hits.jsonl \| sort \| uniq -c`|
|reminders delivered (`remind/hits.jsonl`)|**28 deliveries across 9 distinct ids**, out of **18 distinct rule ids** (24 lines) in `reminders.jsonl`|top id `n2594918537x212` delivered 13×; all 9 delivered ids are real rules, and **9 of the 18 rules have never been delivered once**|`jq -r '.id' ~/.claude/skill-compounder/remind/hits.jsonl \| sort \| uniq -c`; `comm -13 <(jq -r '.id' .../remind/hits.jsonl\|sort -u) <(jq -r '.id' .../reminders.jsonl\|sort -u) \| wc -l`|
|forges|**12 `start`, 6 `done`, 6 `fail`**|1 `apply` row, 1 `verdict` row (a MISFIRE on `watch-ci-run`, whose forge then failed), 3 `escalate`|`jq -r '.event' ~/.claude/skill-compounder/ledger.jsonl \| sort \| uniq -c`|
|notes tier (`skillnote`)|**71 `note` rows**|by scope: project 40, global 13, memory 6, scratch/other 12|`jq -r 'select(.event=="note")\|(.scope//"?")' … \| sort \| uniq -c`|

Ledger event totals (`jq -r '.event' … | sort | uniq -c`, 1153 rows): `use` 1039, `note` 71,
`origin` 13, `start` 12, `fail` 6, `done` 6, `escalate` 3, `verdict` 1, `horizon` 1,
`apply` 1.

Repeat store: **914 rows** (`fail` 509, `recover` 404, `dismiss` 1) over **151 distinct
sessions**. `bash bin/skillrepeat list` shows the two signatures whose GATE column reads
`refuses` are both synthetic red-team fixtures
(`python3 scripts/count_words.py README.md`, `python3 prodpkg/__main__.py …`); every
signature drawn from real work reads `exempt`.

**Read against the maintainer's gate:** the mechanisms that fire on real work are the
statement arm of the lesson gate, the mission, the nudges and the notes tier. The two
newest refusing arms have fired 5 times between them and every one of those 5 was on a
red-team fixture. `doc-gate` (4) and `apply-gate` (1) are the only gates with real fires,
and `apply-gate`'s single fire is the only closed forge loop in the ledger.

---

## 3. Complexity, today vs `v0.3.1`

`v0.3.1` = `b7f6a47`, tagged 2026-09-03 04:03:56 -0400 — **two days ago**.
`__pycache__` excluded throughout (it is gitignored; `/usr/bin/grep` and `wc` will
otherwise count `.pyc` bytes).

```bash
for d in hooks bin statusline skill_compounder; do
  git ls-files $d | grep -v __pycache__ | xargs wc -l | tail -1
  git ls-tree -r --name-only v0.3.1 -- $d | grep -v __pycache__ |
    while read -r f; do git show "v0.3.1:$f" | wc -l; done | paste -sd+ - | bc
done
```

|file|v0.3.1|today|delta|
|-|-|-|-|
|hooks/apply-gate.sh|587|589|+2|
|hooks/claim-gate.sh|899|965|+66|
|hooks/compound-improvement.sh|579|785|+206|
|hooks/doc-gate.sh|1061|1063|+2|
|hooks/hooks.json|161|209|+48|
|hooks/insight-capture.sh|592|593|+1|
|**hooks/mission.sh**|**0**|**885**|**+885 (new file)**|
|hooks/precompact.sh|393|456|+63|
|hooks/remind.sh|453|844|+391|
|**hooks/repeat-gate.sh**|**1134**|**3034**|**+1900**|
|hooks/session-review.sh|1022|1023|+1|
|hooks/skill-use.sh|163|165|+2|
|**hooks/ total (12 files)**|**7044**|**10611**|**+3567 (+50.6%)**|
|bin/skillcontrib|586|1052|+466|
|bin/skillforge|3506|3852|+346|
|bin/skillinsight|1441|1506|+65|
|bin/skillnote|919|1594|+675|
|bin/skillrepeat|637|900|+263|
|bin/skillreport|1381|1708|+327|
|**bin/ total (6 files)**|**8470**|**10612**|**+2142 (+25.3%)**|
|statusline/skillforge-status.sh|716|716|0|
|statusline/statusline.sh|117|117|0|
|**statusline/ total**|**833**|**833**|**0**|
|skill_compounder/installer.py|1991|2530|+539|
|**skill_compounder/ total**|**1991**|**2530**|**+539 (+27.1%)**|
|**ALL FOUR DIRECTORIES**|**18338**|**24586**|**+6248 (+34.1%)**|

No file present at `v0.3.1` is gone today
(`comm -23 <(git ls-tree -r --name-only v0.3.1 …) <(git ls-files …)` prints nothing).

Tests:

```bash
ls tests/test_*.py | wc -l ; ls tests/*.py | xargs wc -l | tail -1
git ls-tree -r --name-only v0.3.1 -- tests | grep -c 'test_.*\.py$'
```

|      |v0.3.1|today|delta|
|-|-|-|-|
|`tests/test_*.py` files|51|54|+3|
|`tests/*.py` lines|42356|51044|**+8688 (+20.5%)**|

Docs (`wc -l docs/*.md README.md`; `docs/media/forge.gif` excluded as binary):

|      |v0.3.1|today|delta|
|-|-|-|-|
|README.md|367|460|+93|
|docs/architecture.md|516|862|+346|
|docs/CLAUDE-CODE-BEHAVIOR.md|982|1421|+439|
|docs/DESIGN.md|1207|1731|+524|
|docs/development.md|85|127|+42|
|docs/e2e.md|156|313|+157|
|docs/measurement.md|139|522|+383|
|docs/operations.md|356|818|+462|
|docs/releasing.md|122|122|0|
|**docs + README total**|**3930**|**6376**|**+2446 (+62.2%)**|

The deliverable itself:

|      |v0.3.1|today|delta|
|-|-|-|-|
|`skills/*/SKILL.md` lines|4530|4514|**-16**|

**Read against the maintainer's fourth condition** — *an existing component is retired or
simplified to make room*: in the two days since `v0.3.1`, machinery grew **+6248 lines
(+34%)**, tests **+8688 (+21%)**, docs **+2446 (+62%)**, and the skills — the actual
product — shrank by 16 lines. Nothing was retired: zero files removed from the four
machinery directories. `hooks/repeat-gate.sh` alone (3034 lines) is now larger than
`statusline/` + `skill_compounder/` combined, and 30% of the whole hooks+bin surface.

---

## 4. Level C — has `skillcontrib propose` ever run for real?

**No. Zero times.**

```bash
grep -c '"event":"contrib"' ~/.claude/skill-compounder/ledger.jsonl   # -> 0
ls -la ~/.claude/skill-compounder/contrib
#   ls: /Users/jmanning/.claude/skill-compounder/contrib: No such file or directory
gh pr list --repo ContextLab/claude-skill-compounder --state all --limit 20
#   7  Implement the roadmap: seed pool, plugin packaging, ledger, insight queue,
#      contribute-back   roadmap-issues-2-6   MERGED   2026-08-25T04:48:56Z
```

The repository has had **one pull request in its entire history**, merged 2026-08-25, and
it is the roadmap PR — not a skill proposal. The `contrib` state directory has never been
created. `bin/skillcontrib`'s own header says so, in the code, as the reason `propose` was
built at all:

> `propose` is the exception, and running it without --dry-run IS the consent. It was
> read-only reconnaissance and a hand-walked procedure for its whole life, and that
> produced 47 reconnaissance runs and zero pull requests: a consent gate a human has to
> walk seven times is a gate nobody reaches the far side of.

`recon` confirmed read-only before running, from the same header:

> EVERY SUBCOMMAND BUT `propose` PERFORMS NO NETWORK WRITES. `dedup`, `whoami`,
> `preflight` and `recon` only read: no forking, no branch creation, no uploading of
> commits, no opening of pull requests.

`bash bin/skillcontrib recon stale-artifact-check`, verbatim:

```
skillcontrib propose
  skill:    stale-artifact-check
  upstream: ContextLab/claude-skill-compounder
  mode:     DRY RUN. Nothing is forked, pushed, opened or recorded.

== step 1/6: locate the skill, parse-check it, read its routing pin
skill directory: /Users/jmanning/claude-skill-compounder/skills/stale-artifact-check
OK: /Users/jmanning/claude-skill-compounder/skills/stale-artifact-check/SKILL.md
  name: stale-artifact-check (matches the directory)
  other files that must travel with it: (none)
  routing pin: measured 2026-09-01, cli 2.1.252 (Claude Code), model sonnet
  result: verified 9/9 must-fire draws, 9/9 must-not-fire draws (3/3 each prompt over 3 runs)

== step 2/6: duplicate check against ContextLab/claude-skill-compounder (tree, pull requests, fuzzy)
repo:        ContextLab/claude-skill-compounder (pass --repo to target a different one)
skill name:  stale-artifact-check  (compared as: staleartifactcheck)
description: Use when an edit you made appears to have had no observable effect, ...

ALREADY UPSTREAM: skills/stale-artifact-check/SKILL.md exists in ContextLab/claude-skill-compounder (default branch main).
Its frontmatter declares name: stale-artifact-check
Improve the existing skill instead of proposing a second copy.
```

Exit code **9** ("the skill already exists in the upstream tree") — correct, since this
checkout IS the upstream. The mechanism works; there is no second repository for it to
work *toward*. Level C has never been exercised end to end against a real target.

---

## 5. Release state

```bash
git tag --sort=-v:refname | head -3     # v0.3.1, v0.3.0   (only two tags exist)
git log --oneline v0.3.1..HEAD | wc -l  # 56
git log -1 --format='%ci %h' v0.3.1     # 2026-09-03 04:03:56 -0400 b7f6a47
```

- **Two tags total**: `v0.3.1`, `v0.3.0`. No `v0.3.2`, no release since 2026-09-03.
- **56 commits on `main` since `v0.3.1`**, in two days, carrying +6248 lines of machinery
  (§3) — all unreleased.

`install.sh` **follows `main` by default** and can be pinned:

```bash
grep -n 'REF\|git pull\|--branch' install.sh
```

- line 35: `REF="${SKILL_COMPOUNDER_REF:-main}"` — the default is the branch tip, not a tag.
- line 321: `git clone --depth 1 --branch "$REF" "$REPO_URL" "$TMP_CLONE"`, falling back at
  line 324-325 to a full clone + `checkout` for a bare SHA.
- line 299-310: there is deliberately **no `git pull`** after checkout; a plain re-run
  re-wires the current checkout and does not move it. `--update` and `--rollback` are the
  only things that move it, and both refuse a checkout the user made themselves (line 233-239).
- `<state>/install-ref` records `current` and `previous` (ref, sha) so `--rollback` has
  somewhere to go (line 187-211).

**An upgrade path between two tags IS tested.**

```bash
grep -rln 'rollback\|upgrade' tests/ docs/
#   tests/test_installer.py  tests/test_seed_destructive.py  tests/test_forge_close_race.py
#   tests/test_repeat_gate.py  tests/test_install_sh.py  tests/test_skillforge.py
#   docs/operations.md  docs/releasing.md  docs/development.md  docs/DESIGN.md
grep -n 'def test.*\(rollback\|update\|upgrade\|ref\)' tests/test_install_sh.py
#   187:  def test_pinned_install_then_update_then_rollback_and_back
```

`tests/test_install_sh.py` builds a real local origin with two synthetic tags (`v0.1`,
`v0.2`, differing by a `RELEASE-MARKER` file), then installs `--ref v0.1`, `--update --ref
v0.2`, `--rollback`, and asserts both HEAD and the *working tree* returned to `v0.1`, plus
the `current`/`previous` swap in the ref record. Its docstring names the gap it closed:

> Issue #38 asked for "a test [that] installs a tag into a temp config, upgrades to a later
> tag, rolls back, and asserts the tree matches the first tag". Nothing here did.

The one thing it does **not** cover: the upgrade is over two *synthetic* tags in a temp
repo, never over `v0.3.0 → v0.3.1` (real trees, real state migration). A user upgrading
across the real tags is exercising an untested path — and there are 56 unreleased commits
waiting to become the next one.

---

**Verification of read-only:** `git status --short` prints nothing and `git log --oneline -1` is
`603e61a` after every command above.
